# Local dev — Vite proxy rewrites /api → localhost:5264, so no env needed here.
# This file exists to show what vars are available; values are set in .env.local (gitignored).
VITE_API_BASE_URL=https://fatalecore-production.up.railway.app/api/
VITE_SIGNALR_URL=https://fatalecore-production.up.railway.app
