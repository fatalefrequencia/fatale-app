# Production — filled in after Railway gives you the public URL.
# Update this URL, then run: git add .env.production && git commit && git push
# Vercel will rebuild automatically and pick up the new API URL.
VITE_API_BASE_URL=https://api.fatale.fm/api/
VITE_SIGNALR_URL=https://api.fatale.fm