# Production — API calls go through same-origin /api/ proxy (see public/_redirects).
# Media + SignalR still use the absolute API host.
VITE_API_BASE_URL=https://api.fatale.fm/api/
VITE_SIGNALR_URL=https://api.fatale.fm
